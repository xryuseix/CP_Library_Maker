	"": {
		"prefix": "",
		"body": [
			"// x,y に ax + by = gcd(a, b) を満たす値が格納される",
			"// 返り値は<gcd(a, b), x, y>",
			"tuple<ll, ll, ll> extgcd(ll a, ll b) {",
			"\tif (b == 0) {",
			"\t\treturn {a, 1, 0};",
			"\t}",
			"\tauto [g, x, y] = extgcd(b, a % b);",
			"\treturn {g, y, x - a / b * y};",
			"}"
		],
		"description": ""
	}